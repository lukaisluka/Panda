# Panda

Panda 是一个 ACP（Agent Client Protocol）通用客户端：连接任意兼容 ACP 的 agent 服务，以消息流为核心体验。Panda 是纯协议客户端——从不拉起 agent 进程，只连接已在运行的服务。

## Language

### 连接与配置

**Agent 配置**:
用户保存的一条连接预设：名称 + WebSocket 地址 + 默认工作目录。一个配置同一时刻最多一条活跃连接；多条配置的连接可并行。
_Avoid_: endpoint、service、连接配置

**活跃连接**:
与 agent 服务建立并保持的一条连接，身份挂在其 Agent 配置上（临时直连除外）。断开一条连接只影响它自己的会话与状态。
_Avoid_: 并行连接（作独立概念时）

**前台会话**:
唯一被主界面渲染的会话。选择任一会话，其所属连接同时成为前台连接，其余活跃连接转后台。
_Avoid_: 当前会话、焦点会话

**后台连接**:
非前台的活跃连接；持续接收更新，可带运行中或需要关注的指示。
_Avoid_: 挂起连接

**临时直连**:
未保存为 Agent 配置的一次性连接；断开即结束，不保留任何状态（其期间发现的会话仍按端点记忆保留——那是端点的记忆，不是这条连接的）。
_Avoid_: 匿名连接

**断开**:
终止一条活跃连接，但保留其槽位、会话列表与文档；可重新连接。
_Avoid_: 移除（混用时）

**移除**:
断开并销毁一条连接的全部本地状态——槽位、会话列表与文档。
_Avoid_: 删除连接、关闭连接

### 状态指示

**运行中**:
一条连接有进行中的 agent turn；每条连接同一时刻最多一个。
_Avoid_: busy

**需要关注**:
后台连接的聚合指示，三源：权限挂起、连接错误、后台完成的 turn 未被前台查看；任一源非空即亮。
_Avoid_: 未读（指代整体时）

**端点**:
Agent 配置中指向 ACP 服务的 WebSocket 地址。它只是配置的一个字段，永远不指代整条配置。
_Avoid_: 服务、server（用作配置整体时）

**自报名称**:
agent 在 `initialize` 响应中报告的自己的名字（`title ?? name`）。区别于用户为 Agent 配置起的名字。
_Avoid_: Agent 名（与配置名混淆时）

### 会话

**会话**:
与某个 agent 的一次对话，由 sessionId 标识，按端点记忆、与服务器列表合并。
_Avoid_: 对话、聊天

### 能力与授权

**有效能力**:
一条连接对某项功能实际可用的判定，由 agent 声明、宿主能力与策略合成；UI 与执行路径共用的唯一真相。
_Avoid_: capability（无修饰泛指时）、实际支持

**宿主能力**:
Panda 宿主自身具备的协议面（权限、会话更新、客户端 MCP、elicitation），按存在性表达，词汇锚定 ACP v2 存活的客户端方法。
_Avoid_: Platform 分片、fs/terminal 分片（v2 已移除的执行面）

**策略**:
权限请求的宿主侧判定，结论只有「交用户决定」或「拒绝」，永不代表用户放行。
_Avoid_: 策略引擎、RBAC、安全策略（泛化时）