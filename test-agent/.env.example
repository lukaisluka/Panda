# JSON 必须保持在一行；可配置多个 OpenAI Chat Completions 兼容模型。
# 不要把真实密钥提交到仓库。
PANDA_TEST_AGENT_REAL_MODELS=[{"value":"openai-compat:example","name":"Example Model","model":"example-model","base_url":"https://example.invalid/v1","api_key":"replace-me"}]
